module.exports = function towelSort (matrix) {
    let arr = [];
    for(i=0; i<matrix.length; i++){
        if(matrix= []){
            return arr;
        }
    if (i % 2 != 0) {
        arr.push(matrix[i].reverse());
    } else {
        arr.push(matrix[i]);
    };
    };    
 return arr.flat();
}
